{
  "name": "ai-fitness-health-analyzer",
  "version": "1.0.0",
  "description": "AI-powered fitness and health analysis application",
  "main": "run.py",
  "scripts": {
    "start": "python run.py",
    "install-deps": "pip install -r requirements.txt && cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm start",
    "clean": "cd frontend && (if exist build rmdir /s /q build) && (if exist node_modules rmdir /s /q node_modules)"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  },
  "author": "",
  "license": "MIT"
}
